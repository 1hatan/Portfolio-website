# Base URL of the Express API.
# In development the Vite proxy (vite.config.js) forwards /api to this URL,
# so you usually don't need to change anything for local dev.
VITE_API_URL=http://localhost:5000
